# Where the app reaches the API. Keep it on the same origin (/api): the session
# is an httpOnly cookie, and a cross-site API would lose it on Safari.
# In dev Vite proxies /api to localhost:3999 (vite.config.ts).
VITE_API_URL=/api
